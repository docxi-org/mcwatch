# Фронт

Сюда кладётся интерфейс на Vue 3 + Vite (`docs/UI-BRIEF.md`).
Собранная статика ожидается в `web/dist` — её отдаёт тот же процесс, что и API.

Каталога `dist` может не быть: сервер это переживёт и будет отдавать только API.
Поэтому в `Dockerfile` копируется `web` целиком, а не `web/dist`.
