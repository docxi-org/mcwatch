/**
 * Имя сервиса. Единственное место: подставляется в логи, `/api/health` и
 * User-Agent. Имя проекта пока в разделе «ждут решения владельца» `TODO.md`.
 */
export const SERVICE_NAME = 'mcwatch';

/**
 * Версия сервиса. Держится здесь, а не читается из `package.json`: тот лежит
 * вне `rootDir` сборки, и импорт ломает раскладку `dist/`. Совпадение с
 * `package.json` стережёт тест — иначе значения разъедутся молча.
 */
export const SERVICE_VERSION = '0.1.0';
